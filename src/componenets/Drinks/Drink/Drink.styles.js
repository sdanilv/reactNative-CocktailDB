import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  drink: {
    marginBottom: 40,
    flexDirection: "row",
    alignItems: "center",
  },
  drinkImg: {
    marginHorizontal: 20,
    height: 100,
    width: 101,
  },
});
